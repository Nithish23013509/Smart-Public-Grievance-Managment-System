spring.application.name=grievance-backend
server.port=${PORT:8080}

# Database Configuration (MySQL - Aiven Cloud)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://grievance-nithish81227-5057.e.aivencloud.com:14068/defaultdb?useSSL=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:avnadmin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=false

# JWT Configuration
jwt.secret=${JWT_SECRET:YTJiM2M0ZDVlNmY3ZzhoOWkwajFrMmwzbTRuNW82cDdxOHI5czB0MXUydjN3NHg1eTZ6N2E4YjljMGQxZTJm}
jwt.expiration-ms=2592000000

# CORS Configuration (comma-separated origins)
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,https://smart-public-grievance-managment-system.onrender.com,https://smart-public-grievance-managment.vercel.app}

# AI Routing Configuration
ai.service.url=${AI_SERVICE_URL:http://localhost:5000}
ai.routing.threshold.auto=0.70
ai.routing.threshold.review=0.40
gemini.api-key=${GEMINI_API_KEY:}
# File Upload Configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
app.file.upload-dir=${UPLOAD_DIR:uploads}
